# profiles/web.local — 站点覆盖层

本目录**可选**。部署脚本(setup.ps1 / setup.sh)在复制模板后,会将本目录内容**递归合并**到目标 profile 之上:

- 同名文件覆盖模板中的文件(例如自定义 `settings.yaml` 片段、额外的依赖声明);
- 本目录不存在时,部署脚本自动跳过此步;
- 本目录内容会进入仓库,请勿放入任何 key / token / 凭据。
